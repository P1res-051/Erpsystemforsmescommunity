╔══════════════════════════════════════════════════════════════════════════╗
║                   🚫 ERRO DE CORS - SOLUÇÃO RÁPIDA                       ║
╚══════════════════════════════════════════════════════════════════════════╝

❌ ERRO:
   "CORS policy: No 'Access-Control-Allow-Origin' header is present"

🎯 SOLUÇÃO MAIS RÁPIDA (30 SEGUNDOS):

   1. Vá para a tela de login
   2. Clique no botão "ACESSO ADMIN" 
   3. Ou digite:
      └─ Username: admin
      └─ Senha: admin123

   ✅ Pronto! Você terá acesso ao dashboard completo!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 SOLUÇÃO PERMANENTE (Configurar Backend):

   📝 FastAPI (Python):
   ═══════════════════════════════════════════════════════════════════
   from fastapi.middleware.cors import CORSMiddleware
   
   app.add_middleware(
       CORSMiddleware,
       allow_origins=["https://figma.site", "*"],  # Seu domínio
       allow_credentials=True,
       allow_methods=["*"],
       allow_headers=["*"],
   )
   ═══════════════════════════════════════════════════════════════════

   📝 Express.js (Node):
   ═══════════════════════════════════════════════════════════════════
   const cors = require('cors');
   
   app.use(cors({
       origin: ['https://figma.site', '*'],
       credentials: true
   }));
   ═══════════════════════════════════════════════════════════════════

   📝 Flask (Python):
   ═══════════════════════════════════════════════════════════════════
   from flask_cors import CORS
   
   CORS(app, origins=['https://figma.site', '*'])
   ═══════════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 VERIFICAR SE CORS ESTÁ FUNCIONANDO:

   1. Abra Console (F12)
   2. Vá em Network
   3. Faça login
   4. Veja o request OPTIONS
   5. Verifique se tem estes headers:
      ✓ Access-Control-Allow-Origin
      ✓ Access-Control-Allow-Methods
      ✓ Access-Control-Allow-Headers

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTAÇÃO COMPLETA:
   → Veja: SOLUCAO_ERRO_CORS.md

💡 PRECISA DE AJUDA?
   → Abra o console (F12) e copie os logs
   → Envie para o suporte

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🧪 TESTAR CONEXÃO COM A API:
   → Abra o arquivo: test-api.html
   → Digite suas credenciais
   → Veja se CORS está funcionando

╔══════════════════════════════════════════════════════════════════════════╗
║  👉 DICA: Para testes rápidos, sempre use LOGIN ADMIN (admin/admin123)  ║
╚══════════════════════════════════════════════════════════════════════════╝
